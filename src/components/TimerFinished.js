import React from 'react';
import './TimerFinished.css';

const TimerFinished = (props) => {
    return(
        <h2 className="TimerFinished">Timer Finished!</h2>
    );
}

export default TimerFinished;